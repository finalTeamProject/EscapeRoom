<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noexit.app.mapper.MyReservationMapper">

	<!-- 반복해서 사용할 조회 쿼리 SQL 조각 구성 -->
	<sql id="myReservationQuery">
		SELECT VRA.RESERVATION_ID, VRA.CAFE_ID, VRA.CAFE_NAME, VRA.ROOM_ID,
		VRA.ROOM_NAME, VRA.TOTAL_MEMBER
		, CASE WHEN VRA.LEADER_ID = #{userId}
		THEN '파티장' ELSE '파티원'
		END AS PARTY_ROLE
		, CASE
		WHEN RC.RESERVATION_ID IS NOT NULL THEN '예약 취소' 
	        <![CDATA[
	        	WHEN VRA.OPEN_AT_DT < SYSDATE THEN  '플레이 완료'
	        ]]>
		ELSE '예약 중'
		END AS RES_STATUS
		, VRA.RES_OPEN_ID, VRA.OPEN_AT, VRA.BOOKED_AT
		, FN_GET_ATTEND_STATUS(VRA.RESERVATION_ID, #{userId}) AS ATTEND_STATUS
		, CASE
		WHEN RC.RESERVATION_ID IS NULL THEN NULL
		WHEN FN_GET_USER_ROLE(VRA.RES_OPEN_ID, RC.USER_ID, RC.CANCEL_AT) = 'USER'
		THEN '취소 완료'
		WHEN FN_GET_USER_ROLE(VRA.RES_OPEN_ID, RC.USER_ID, RC.CANCEL_AT) IN
		('OWNER', 'MANAGER')
		THEN '매장 취소'
		ELSE '알 수 없음'
		END AS CANCEL_TYPE
		, RC.CANCEL_AT AS CANCELED_AT
		FROM VW_RESERVATION_ALL VRA
		LEFT JOIN RESERVATION_CANCEL RC
		ON VRA.RESERVATION_ID = RC.RESERVATION_ID
		WHERE VRA.LEADER_ID = #{userId}
		OR VRA.PARTY_ID IN (
		SELECT PA.PARTY_ID
		FROM PARTY_MEMBER PM
		JOIN PARTY_APPLY PA
		ON PM.APPLY_ID = PA.APPLY_ID
		WHERE PA.USER_ID = #{userId}
		AND NOT EXISTS(
		SELECT PK.MEMBER_ID
		FROM PARTY_KICK PK
		WHERE PK.MEMBER_ID = PM.MEMBER_ID
		)
		)
	</sql>

	<!-- 예약 중인 건 갯수 -->
	<select id="bookedCount" parameterType="Long"
		resultType="Integer">
		SELECT COUNT(*)
		FROM (
		<include refid="myReservationQuery" />
		)
		WHERE RES_STATUS = '예약 중'

	</select>

	<!-- 플레이 완료인 건 갯수 -->
	<select id="doneCount" parameterType="Long" resultType="Integer">
		SELECT COUNT(*)
		FROM (
		<include refid="myReservationQuery" />
		)
		WHERE RES_STATUS = '플레이 완료'

	</select>

	<!-- 예약 취소인 건 갯수 -->
	<select id="canceledCount" parameterType="Long"
		resultType="Integer">
		SELECT COUNT(*)
		FROM (
		<include refid="myReservationQuery" />
		)
		WHERE RES_STATUS = '예약 취소'

	</select>


	<!-- 예약 중인 목록 조회 -->
	<select id="bookedList" parameterType="map"
		resultType="com.noexit.app.model.MyReservationDTO">
		SELECT RESERVATION_ID, CAFE_ID, CAFE_NAME, ROOM_ID, ROOM_NAME,
		OPEN_AT, TOTAL_MEMBER, PARTY_ROLE
		FROM (
		<include refid="myReservationQuery" />
		)
		WHERE RES_STATUS = '예약 중'
		ORDER BY OPEN_AT DESC
		OFFSET #{offset} ROWS
		FETCH FIRST #{size} ROWS ONLY

	</select>

	<!--플레이완료된 목록 조회 -->
	<select id="doneList" parameterType="map"
		resultType="com.noexit.app.model.MyReservationDTO">
		SELECT RESERVATION_ID, CAFE_ID, CAFE_NAME, ROOM_ID, ROOM_NAME,
		OPEN_AT, ATTEND_STATUS
		FROM (
		<include refid="myReservationQuery" />
		)
		WHERE RES_STATUS = '플레이 완료'
		ORDER BY OPEN_AT DESC
		OFFSET #{offset} ROWS
		FETCH FIRST #{size} ROWS ONLY
	</select>


	<!--예약 취소된 목록 조회 -->
	<select id="canceledList" parameterType="map"
		resultType="com.noexit.app.model.MyReservationDTO">

		SELECT RESERVATION_ID, CAFE_ID, CAFE_NAME, ROOM_ID, ROOM_NAME,
		OPEN_AT, CANCEL_TYPE, CANCELED_AT
		FROM (
		<include refid="myReservationQuery" />
		)
		WHERE RES_STATUS = '예약 취소'
		ORDER BY OPEN_AT DESC
		OFFSET #{offset} ROWS
		FETCH FIRST #{size} ROWS ONLY
	</select>


</mapper> 